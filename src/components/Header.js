import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <header data-testid="page-not-found">
        oi
      </header>
    );
  }
}

export default Header;
